# Core UDA Requirements
# Essential packages for Universal Deployment Agent

# Socket.IO for Kit Server communication
python-socketio>=5.8.0
requests>=2.31.0

# Core dependencies
asyncio-mqtt>=0.16.1
protobuf>=4.25.1

# Velocitas SDK for SDV Runtime integration
velocitas-sdk